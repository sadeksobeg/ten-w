# Use this folder as the deploy root for: Deploy manually → drag site/ or site.zip
# After extract: /opt/build/repo/package.json and netlify.toml must sit together.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
